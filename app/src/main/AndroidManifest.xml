<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.androidapp">

    <uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ahd"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ahd"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>
        <activity android:name=".mainactivity.MainActivity" />
        <activity android:name=".activity.ProductListActivity" />
        <activity android:name=".activity.ProductDetailActivity" />
        <activity android:name=".activity.BrandActivity" />
        <activity android:name=".activity.WebViewActivity2" />
        <activity android:name=".activity.TalentDetailActivity" />
        <activity
            android:name=".activity.Maganizetype"
            android:launchMode="singleTask" />
        <activity
            android:name=".activity.MaginazeActivity"
            android:launchMode="singleTask" />
        <activity android:name=".activity.ShopCartActivity" />
        <activity android:name=".activity.BaisiRecommend" />
        <activity android:name=".activity.JumptoWeiBo"></activity>
    </application>

</manifest>